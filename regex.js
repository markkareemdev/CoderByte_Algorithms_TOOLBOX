var names = " is a good boy mark";



// //performing normal string search
// var searcher1 = names.search("mark");

// // using regex to search, the i means case insensitive.
// var searcher2 = names.search(/MARK/i);

// // performing a replace operation on the string without regex.
// var searcher3 = names.replace("good", 'bad');
// var searcher4 = names.replace("boy", 'girl');
// console.log(searcher1, searcher2, searcher3 , searcher4);

// performing a replace on the string using regex
// const replaceRegex = /GOOD/i;
// console.log(names.replace(replaceRegex, "bad"))



// performing a test on the string using a regex

// const validate_regex =  /^[A-Za-z]\w+[A-Za-z0-9]$/;
// console.log(validate_regex.test(names))